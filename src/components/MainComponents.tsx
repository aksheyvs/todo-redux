import FormControl from "./main/FormControl";
import TodoListSection from "./main/TodoListSection";

function MainComponents() {
    return (
        <main>
            <FormControl />
            <TodoListSection />
        </main>
    );
}

export default MainComponents;
